{% extends 'base.html.twig' %}

{% block title %}Adoptant{% endblock %}

{% block body %}
    <h1>Adoptant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adoptant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ adoptant.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adoptant_index') }}">back to list</a>

    <a href="{{ path('app_adoptant_edit', {'id': adoptant.id}) }}">edit</a>

    {{ include('adoptant/_delete_form.html.twig') }}
{% endblock %}
