{% extends 'base.html.twig' %}

{% block title %}Adoptant{% endblock %}

{% block body %} 
   <div class= "cardAnimal">
    <h1>Fiche adoptant</h1>

                <p>Id : {{ adoptant.id }}</p>
            </tr>
            <tr>
                <p>Nom : {{ adoptant.nom }}</p>
                <p>Animal adopté : 
                <td>{% if adoptant.animal %}
  {{ adoptant.animal.nom }}
{% else %}
  0
{% endif %}
</p>
</div>

    <a href="{{ path('app_adoptant_index') }}">back to list</a>

    <a href="{{ path('app_adoptant_edit', {'id': adoptant.id}) }}">edit</a>

    {{ include('adoptant/_delete_form.html.twig') }}
{% endblock %}
